PORT=5000
MONGODB_URI=mongodb://localhost:27017/bvm_queue_management
JWT_SECRET=change_this_secret
JWT_EXPIRE=24h
NODE_ENV=development

# Notes:
# - Replace MONGODB_URI with your MongoDB connection string (Atlas or local).
# - Copy this file to .env before running the server: `cp .env.sample .env`